apply from: rootProject.file('buildsystem/android-library.gradle')

dependencies {
    implementation project(":core")
    implementation project(":core:network")
    implementation project(":features:glide")

    implementation(
            deps.kotlin.stdlib,
            deps.android.appcompat,
            deps.android.ktx,
            deps.android.fragment,
            deps.android.constraint,
            deps.material.material,
            deps.navigation.fragment,
            deps.navigation.ui,
            deps.rxJava.rx2,
            deps.rxJava.rxAndroid,
            deps.network.retrofit2,
            deps.network.moshi,
            deps.glide.glide,
            deps.tools.timber,
            deps.dagger.dagger
    )

    kapt(
            deps.dagger.compiler,
            deps.glide.compiler,
            deps.network.moshicodegen
    )
}
